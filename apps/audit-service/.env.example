# Runtime connection — audit_app role (USAGE only, no CREATE; INSERT+SELECT only — append-only)
DATABASE_URL=postgres://audit_app:change_me@localhost:5432/postgres

# Migration-only connection — audit_migrator role (USAGE + CREATE)
MIGRATION_DATABASE_URL=postgres://audit_migrator:change_me@localhost:5432/postgres

KAFKA_BROKERS=localhost:9092,localhost:9095,localhost:9096
PORT=3004
