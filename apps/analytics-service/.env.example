# Runtime connection — analytics_app role (USAGE only, no CREATE)
DATABASE_URL=postgres://analytics_app:change_me@localhost:5432/postgres

# Migration-only connection — analytics_migrator role (USAGE + CREATE)
MIGRATION_DATABASE_URL=postgres://analytics_migrator:change_me@localhost:5432/postgres

KAFKA_BROKERS=localhost:9092,localhost:9095,localhost:9096
PORT=3003
