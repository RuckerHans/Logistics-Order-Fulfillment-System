# Section 6's dependency list. Two Postgres drivers is intentional, not
# redundant: asyncpg for the running app (async, matches aiokafka/FastAPI),
# psycopg2-binary for Alembic (its migration runner is sync-only) — see the
# "Why two Postgres drivers" note in docs/project-plan.md Section 6.
fastapi==0.115.0
uvicorn[standard]==0.30.6
aiokafka==0.11.0
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
pydantic==2.9.2
python-dotenv==1.0.1
alembic==1.13.2
psycopg2-binary==2.9.9
