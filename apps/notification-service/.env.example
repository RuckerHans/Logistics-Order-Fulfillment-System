RABBITMQ_URL=amqp://localhost:5672

# Runtime connection — notification_app role (USAGE only, no CREATE)
DATABASE_URL=postgres://notification_app:change_me@localhost:5432/postgres

# Migration-only connection — notification_migrator role (USAGE + CREATE)
MIGRATION_DATABASE_URL=postgres://notification_migrator:change_me@localhost:5432/postgres

PORT=3002
