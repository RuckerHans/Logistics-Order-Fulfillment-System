RABBITMQ_URL=amqp://localhost:5672

# Runtime connection — inventory_app role (USAGE only, no CREATE)
DATABASE_URL=postgres://inventory_app:change_me@localhost:5432/postgres

# Migration-only connection — inventory_migrator role (USAGE + CREATE)
MIGRATION_DATABASE_URL=postgres://inventory_migrator:change_me@localhost:5432/postgres

PORT=3001
